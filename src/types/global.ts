export interface AppSettings {
  title: string
  showSettings: boolean
  fixedHeader: boolean
  tagsView: boolean
  sidebarLogo: boolean
  layout: string
  themeColor: string
  theme: string
  size: string
}
